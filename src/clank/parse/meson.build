# SPDX-License-Identifier: Apache-2.0

library_hdrs_parse = files([

])

library_srcs += files([

])

if not meson.is_subproject()
	install_headers(
		library_hdrs_parse,
		subdir: 'clank' / 'parse'
	)
endif
